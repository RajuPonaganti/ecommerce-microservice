server:
  port: 8082

spring:
  config:
    import: "configserver:"
  profiles:
    active: dev
  application:
    name: product-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      label: main
      discovery:
        enabled: true
        service-id: config-server
