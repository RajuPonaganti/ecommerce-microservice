server:
  port: 8083

spring:
  config:
    import: "configserver:"
  profiles:
    active: dev
  application:
    name: order-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      label: main
      discovery:
        enabled: true
        service-id: config-server


