server:
  port: 8089

spring:
  application:
    name: auth-service
  profiles:
    active: dev
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: false
      label: main
      discovery:
        enabled: true
        service-id: config-server