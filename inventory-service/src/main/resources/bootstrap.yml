server:
  port: 8081

spring:
  profiles:
    active: dev
  application:
    name: inventory-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
      label: main
      discovery:
        enabled: true
        service-id: config-server
