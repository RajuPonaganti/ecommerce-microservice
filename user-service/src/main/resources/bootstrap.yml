server:
  port: 8085

spring:
  config:
    import: "configserver:"
  profiles:
    active: dev
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server
