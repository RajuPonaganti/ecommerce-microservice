server:
  port: 8088

spring:
  config:
    import: "configserver:"
  profiles:
    active: dev
  application:
    name: notification-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server